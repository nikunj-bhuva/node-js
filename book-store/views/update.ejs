<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f2f5;
        }
        .form-container {
            background-color: #fff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            max-width: 500px;
        }
    </style>
</head>
<body>
    <div class="container my-5">
        <h2 class="text-center text-warning mb-4">Edit Book</h2>
        <div class="form-container mx-auto">
            <form action="/editData/<%= storeData.id %>" method="post">
                <div class="mb-3">
                    <label for="bookName" class="form-label">Book Title</label>
                    <input type="text" name="bookName" value="<%= storeData.bookName %>" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label for="Bookcatagary" class="form-label">Book Category</label>
                    <select name="Bookcatagary" class="form-select" required>
                        <option value="History" <%= storeData.Bookcatagary === "History" ? "selected" : "" %>>History</option>
                        <option value="Science Fiction" <%= storeData.Bookcatagary === "Science Fiction" ? "selected" : "" %>>Science Fiction</option>
                        <option value="Biography" <%= storeData.Bookcatagary === "Biography" ? "selected" : "" %>>Biography</option>
                        <option value="Mystery" <%= storeData.Bookcatagary === "Mystery" ? "selected" : "" %>>Mystery</option>
                        <option value="Business" <%= storeData.Bookcatagary === "Business" ? "selected" : "" %>>Business</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="bookprice" class="form-label">Book Price (₹)</label>
                    <input type="text" name="bookprice" value="<%= storeData.bookprice %>" class="form-control" required>
                </div>
                <button type="submit" class="btn btn-warning w-100">Save Changes</button>
            </form>
        </div>
    </div>
</body>
</html>